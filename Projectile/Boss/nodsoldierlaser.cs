using System;
using System.Collections.Generic;
using System.Text;

using Terraria;
using TAPI;

namespace HallowedEnd {
    public class nodsoldierlaser : ModProjectile {
        public nodsoldierlaser(ModBase modbase, Projectile P) : base(modbase, P) { }
    }
}
